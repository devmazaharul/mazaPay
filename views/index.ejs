<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Complete Your Payment | MazaPay</title>
    <style>
      :root {
        --primary: #e60076;
        --primary-hover: #c20063;
        --bg: #f8f9fb;
        --card-bg: #ffffff;
        --border: #e5e7eb;
        --text: #1f2937;
        --muted: #6b7280;
        --success-bg: #e6fffa;
        --success-text: #065f46;
        --error-bg: #fee2e2;
        --error-text: #b91c1c;
      }

      * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
      }

      body {
        font-family: "Inter", sans-serif;
        background: linear-gradient(135deg, #fce7f3, #f9fafb);
        min-height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
        color: var(--text);
        padding: 20px;
      }

      .container {
        background: var(--card-bg);
        border-radius: 16px;
        padding: 40px 35px;
        box-shadow: 0 8px 25px rgba(0, 0, 0, 0.08);
        width: 100%;
        max-width: 450px;
        text-align: center;
        animation: fadeIn 0.3s ease-in-out;
      }

      @keyframes fadeIn {
        from {
          opacity: 0;
          transform: translateY(10px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      h1 {
        font-size: 28px;
        font-weight: 800;
        color: var(--primary);
        margin-bottom: 10px;
      }

      .description {
        font-size: 15px;
        color: var(--muted);
        margin-bottom: 25px;
      }

      .merchant-info {
        text-align: left;
        background: #fdf2f8;
        border: 1px solid var(--border);
        border-radius: 12px;
        padding: 15px 18px;
        margin-bottom: 25px;
      }

      .merchant-info p {
        margin: 6px 0;
        font-size: 15px;
      }

      .merchant-info span {
        font-weight: 600;
        color: var(--primary);
        width: 130px;
        display: inline-block;
      }

      label {
        display: block;
        text-align: left;
        margin-bottom: 8px;
        font-size: 14px;
        font-weight: 600;
        color: var(--text);
      }

      input {
        width: 100%;
        padding: 12px;
        border: 1px solid var(--border);
        border-radius: 8px;
        font-size: 15px;
        background: #f9fafb;
        color: var(--text);
        margin-bottom: 18px;
        transition: 0.2s ease;
      }

      input:focus {
        border-color: var(--primary);
        box-shadow: 0 0 0 2px rgba(230, 0, 118, 0.2);
        outline: none;
      }

      .button-wrapper {
        display: flex;
        justify-content: space-between;
        gap: 15px;
        margin-top: 20px;
      }

      button {
        flex: 1;
        padding: 12px;
        border: none;
        border-radius: 8px;
        font-weight: 600;
        font-size: 15px;
        cursor: pointer;
        transition: all 0.25s ease;
      }

      #confirm {
        background: var(--primary);
        color: #fff;
      }

      #confirm:hover {
        background: var(--primary-hover);
        transform: translateY(-2px);
      }

      #cancel {
        background: #f3f4f6;
        color: var(--text);
      }

      #cancel:hover {
        background: #e5e7eb;
      }

      #messageBox {
        display: none;
        margin-top: 20px;
        padding: 14px;
        border-radius: 8px;
        font-size: 14px;
      }

      .success {
        background: var(--success-bg);
        color: var(--success-text);
      }

      .error {
        background: var(--error-bg);
        color: var(--error-text);
      }

      footer {
        margin-top: 25px;
        font-size: 12px;
        color: var(--muted);
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>Complete Payment</h1>
      <p class="description">Please confirm your payment details to continue.</p>

      <div class="merchant-info">
        <p><span>Merchant Name:</span> <%= marchenName %></p>
        <p><span>Merchant ID:</span> <%= paymentId %></p>
        <p><span>Amount:</span> <%= amount %> BDT</p>
      </div>

      <form id="paymentForm">
        <input type="hidden" id="paymentId" value="<%= paymentId %>" />
        <input type="hidden" id="marchentName" value="<%= marchenName %>" />
        <input type="hidden" id="amount" value="<%= amount %>" />
        <input type="hidden" id="webhookURL" value="<%= webhookURL %>" />
        <input type="hidden" id="successURL" value="<%= successURL %>" />
        <input type="hidden" id="faildURL" value="<%= faildURL %>" />

        <label for="email">Your MazaPay Email</label>
        <input type="email" id="email" name="email" placeholder="example@gmail.com" required />

        <label for="pin">Your PIN</label>
        <input type="password" id="pin" name="pin" placeholder="Enter PIN" minlength="4" maxlength="6" required />

        <div class="button-wrapper">
          <button type="submit" id="confirm">Confirm Payment</button>
          <button type="button" id="cancel">Cancel</button>
        </div>

        <div id="messageBox"></div>
      </form>

      <footer>© <%= new Date().getFullYear() %> MazaPay. All rights reserved.</footer>
    </div>

    <script src="/js/script.js"></script>
  </body>
</html>
