<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Complete Your Payment</title>
    <style>
      :root {
        --bg-color: #f3f4f6;
        --card-bg: #ffffff;
        --border-color: #e5e7eb;
        --shadow-color: rgba(0, 0, 0, 0.1);
        --text-dark: #111827;
        --text-gray: #6b7280;
        --input-border: #d1d5db;
        --primary-color: #ec4899; /* Pink color */
        --primary-hover: #d946ef;
        --cancel-bg: #e5e7eb;
        --cancel-hover: #d1d5db;
        --success-bg: #d1fae5;
        --success-text: #065f46;
        --error-bg: #fee2e2;
        --error-text: #b91c1c;
      }
      * {
        box-sizing: border-box;
      }
      body {
        font-family: 'Inter', sans-serif;
        background-color: var(--bg-color);
        margin: 0;
        padding: 20px;
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
      }
      .container {
        background-color: var(--card-bg);
        border: 1px solid var(--border-color);
        border-radius: 12px;
        box-shadow: 0 8px 20px var(--shadow-color);
        padding: 40px;
        width: 100%;
        max-width: 450px;
        text-align: center;
      }
      .title {
        font-size: 28px;
        font-weight: 800;
        color: var(--text-dark);
        margin-bottom: 8px;
      }
      .description {
        font-size: 15px;
        color: var(--text-gray);
        margin-bottom: 30px;
      }
      .merchant-info {
        font-size: 15px;
        color: var(--text-dark);
        background-color: #f9fafb;
        border: 1px solid var(--border-color);
        border-radius: 8px;
        padding: 15px;
        margin-bottom: 25px;
      }
      .merchant-info p {
        margin: 5px 0;
        display: flex;
        justify-content: space-between;
      }
      .merchant-info strong {
        font-weight: 700;
        color: var(--text-dark);
      }
      form {
        text-align: left;
      }
      label {
        display: block;
        margin-bottom: 8px;
        font-size: 14px;
        font-weight: 600;
        color: var(--text-dark);
      }
      input {
        width: 100%;
        padding: 12px;
        border: 1px solid var(--border-color);
        border-radius: 8px;
        font-size: 15px;
        color: var(--text-dark);
        background-color: #f9fafb;
        outline: none;
        transition: all 0.2s ease;
        margin-bottom: 20px;
      }
      input:focus {
        border-color: var(--primary-color);
        box-shadow: 0 0 0 2px rgba(236, 72, 153, 0.2);
      }
      .button-wrapper {
        display: flex;
        justify-content: space-between;
        gap: 15px;
        margin-top: 20px;
      }
      button {
        flex: 1;
        padding: 12px;
        border: none;
        border-radius: 8px;
        font-weight: bold;
        font-size: 15px;
        cursor: pointer;
        transition: all 0.2s ease;
      }
      #confirm {
        background-color: var(--primary-color);
        color: white;
      }
      #confirm:hover {
        background-color: var(--primary-hover);
      }
      #cancel {
        background-color: var(--cancel-bg);
        color: var(--text-dark);
        width: 40%;
      }
      #cancel:hover {
        background-color: var(--cancel-hover);
      }
      #messageBox {
        display: none;
        margin-top: 20px;
        padding: 15px;
        border-radius: 8px;
        font-size: 14px;
      }
      .success {
        background-color: var(--success-bg);
        color: var(--success-text);
      }
      .error {
        background-color: var(--error-bg);
        color: var(--error-text);
      }
    </style>
  </head>
 <body>
    <div class="container">
      <h1>Complete Payment</h1>
      <p class="description">Confirm your payment details and proceed.</p>

      <div class="merchant-info">
        <p><span>Merchant Name:</span> <%= marchenName %></p>
        <p><span>Merchant ID:</span> <%= paymentId %></p>
        <p><span>Amount:</span> <%= amount %> BDT</p>
      </div>

    <form id="paymentForm">
  <input type="hidden" id="paymentId" name="paymentId" value="<%= paymentId %>" />
  <input type="hidden" id="marchentName" name="marchentName" value="<%= marchenName %>" />
  <input type="hidden" id="amount" name="amount" value="<%= amount %>" />

  <!-- ✅ নতুন ৩টা hidden field -->
  <input type="hidden" id="webhookURL" name="webhookURL" value="<%= webhookURL %>" />
  <input type="hidden" id="successURL" name="successURL" value="<%= successURL %>" />
  <input type="hidden" id="faildURL" name="faildURL" value="<%= faildURL %>" />

  <label for="email"><b>Your mazaPay Email</b></label>
  <input type="email" id="email" name="email" required placeholder="example@gmail.com" />

  <label for="pin"><b>Your PIN</b></label>
  <input type="password" id="pin" name="pin" placeholder="Enter PIN" minlength="4" maxlength="6" required />

  <div id="btnWrapper">
    <button type="submit" id="confirm">Confirm Payment</button>
    <button type="button" id="cancel">Cancel</button>
  </div>

  <div id="messageBox"></div>
</form>

    </div>


<script src="/js/script.js"></script>

  </body>
</html>
</html>
