<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Payment Expired | MazaPay</title>
  <style>
    /* ===== Global Reset ===== */
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: 'Inter', Arial, sans-serif;
      background: linear-gradient(135deg, #f8f9fa, #eef1f4);
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      color: #212529;
    }

    .container {
      background: #ffffff;
      padding: 40px 30px;
      border-radius: 16px;
      box-shadow: 0 8px 25px rgba(0, 0, 0, 0.08);
      max-width: 420px;
      width: 100%;
      text-align: center;
      animation: fadeIn 0.4s ease-in-out;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(10px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .expired-icon {
      font-size: 50px;
      color: #dc3545;
      margin-bottom: 10px;
    }

    .expired-title {
      font-size: 26px;
      font-weight: 700;
      color: #b02a37;
      margin-bottom: 15px;
    }

    .merchant-info {
      text-align: left;
      margin: 25px 0;
      font-size: 15px;
      color: #333;
      background: #f8f9fa;
      border-radius: 10px;
      padding: 15px;
    }

    .merchant-info p {
      margin: 8px 0;
      line-height: 1.5;
    }

    .merchant-info span {
      font-weight: 600;
      color: #e60076;
      display: inline-block;
      width: 120px;
    }

    .expired-msg {
      background: #fff4f4;
      border: 1px solid #f5c2c7;
      padding: 14px;
      border-radius: 10px;
      color: #842029;
      font-weight: 500;
      margin-top: 15px;
    }

    .back-btn {
      display: inline-block;
      margin-top: 25px;
      padding: 10px 20px;
      border-radius: 8px;
      border: none;
      background: #e60076;
      color: #fff;
      font-size: 15px;
      font-weight: 500;
      text-decoration: none;
      cursor: pointer;
      transition: all 0.2s ease-in-out;
    }

    .back-btn:hover {
      background: #cc0066; /* slightly darker pink for hover */
      transform: scale(1.03);
    }

    footer {
      margin-top: 20px;
      font-size: 12px;
      color: #6c757d;
    }

  </style>
</head>

  <div class="container">
    <div class="expired-icon">⏰</div>
    <div class="expired-title">Payment Expired</div>

    <div class="merchant-info">
      <p><span>Merchant Name:</span> E-commerce Shop</p>
      <p><span>Merchant ID:</span> MPY-1234567890</p>
      <p><span>Amount:</span> 5500 BDT</p>
    </div>

    <div class="expired-msg">
      The payment window has timed out. Please initiate a new transaction from the merchant site.
    </div>

<button 
  class="back-btn" 
  id="backButton"  data-fail-url="<%= faildURL %>" 
>
  ⬅ Back
</button>

    <footer>© 2025 MazaPay. All rights reserved.</footer>
  </div>

<script src="/js/script.js" ></script>
</body>
</html>